# 検証・コミット前レビュー

2026-09-22。主担当による自己レビュー。Claudeへの独立案出しは認証エラーで未取得。独立レビュー済みとは扱わない。

## 対象

3枚12案の画像、2枚の原本、README、SOURCES、JOB-RECORD、prompts.json。

## 確認結果

- PASS：A1〜A4はすべてClaudeの上に雲。可愛さ重視の依頼に合う角丸・短い脚・単純な目を確認。
- PASS：B1〜B4は素体／座り／抱っこ／ペアで形態が異なる。
- PASS：C1〜C4はアプリアイコン／切抜き輪郭／単色コイン／黒い置物で比較できる。
- PASS：画像のラベルA1〜C4とREADMEの説明が一致。生成画像を目視済み。
- PASS：3Dデータや印刷検証済みとの誤認を避け、未実施工程を明示。
- PASS：元画像・既存プロジェクト・材料記録は保持。変更は今回の専用フォルダーとVault記録のみ。

## 採用した指摘・補足

1. 四本脚の位置が案ごとに異なる。単一モデルの複数ビューではないと明示。選定後に配置を確定。
2. A1の雲幅はプロンプト指定の比率より大きい。実寸図ではなく外観案として許容し、比率を測定済みとは書かない。
3. 生成画像のCodexの花輪郭は原本と完全一致しない。CADでは原本をトレースする旨を明示。
4. 在庫の青は半透明PLAで画像は不透明な青。色と質感の違いを明示。
5. A2の頭の重さ、A4の長い帽子先は未解決の製作課題。接触面・軽量化・支持の検討を残し、印刷可能とは断言しない。
6. 「もちもち」の外観をTPU機能確認と誤認しないよう追記。
7. 最初のGit差分点検は引用内のMarkdown改行用末尾スペース3か所を検出した。空の引用行へ置換し、追加差分と全履歴範囲で余分な空白が残っていないことを再確認。

## 検証の範囲

画像と文書のみで実行可能な振る舞いを変えないためTDDは対象外。画像ファイル検査、参照整合、パス・秘密値の点検を実施。数値結果はvalidation.json。

## ChatGPT追加案 D1〜D8

主担当による自己レビュー。継続しているClaude認証エラーは再試行せず、独立レビュー済みとは扱わない。

- PASS：D1〜D8の8個の番号が採用画像とCHATGPT.mdで一致。A〜Cの既存番号・画像は保持。
- PASS：ロゴ彫刻・おすわり・抱っこ・ペア・吹き出し・コイン・木彫り・小石形の異なる外観を用意。
- 修正済み：D1〜D4初版の背景の暗さとラベルの読みづらさ。白い背景へ修正した採用版を目視。
- 一部修正・制限明示：D6〜D8の不要な中央凸部を除去。ロゴの線のつながりは依然簡略化されている。正確な複製・CAD原本として使わず、造形時に参照原本から輪郭を作ると明示。
- 制限明示：D1の中央空間やループも原本と完全一致ではない。外観案として保存し、モデル化時の検討項目にした。
- 制限明示：D7の木目やD1の柔らかそうな外観を、ウッドPLAの表面品質や弾性の確認済みとは扱わない。
- PASS：文書・参照追加のみ。TDD対象外、PNG・参照・ID・旧画像の保持を検証。結果はvalidation-chatgpt.json。

## 48mm実モデル・印刷準備

2026-09-22。D1〜D4/B4を各1点。形状は独立レビューで3所見（D3挿入経路、受けの隙間、GLB単位/軸）を採用し、修正後実出力の確認で未解消0となった。根拠は `production-v1/geometry-review.md`。スライス補助は子が実装、主担当が別担当として実データとコードをレビューした。

最終22テスト、23 STL、5組立3MF、5GLB、5色実スライスを確認。層データ・ドキュメント・出典・数量・48mm指定・材料台帳との対応と、既存コンセプト保持を主担当が自己レビュー。CLIの配置はZ回転を含むため、コメントの「平行移動のみ」を修正し、全最終出力の接地面維持を実transformで確認。公開ライセンスを勝手に付与せず、実機・公開の未実施を明記。


## 訂正後A1〜A4追加・既存保持

今回の選定はA1/A2/A3/A4/B4/D3。旧5モデルと旧ZIPは保存基準118ファイルと実行時201ファイルのSHAで不変。新A4種は全て高さ48mm、追加16部品。雲の背面薄膜とA3の面積0facetの独立レビュー2所見を採用し、別実行で最終STLの修正を確認、未解消0。詳細はcloud-addition-v1/geometry-review.md。

最終25テスト、32個の印刷/組立STLの非退化面、4組立3MF、4GLBとプレビューを確認。訂正後28部品を4色へ実スライスし、全物体の接地・層番号連続性・ベッド内押出経路を検査。A3の最終修正後は変更SHAがA3_cloudだけと確認し、白プレートだけ再スライス、他3色を再利用。5時間16分06秒/108.40gはスライサー見積り。

主担当の自己レビューは範囲訂正、元モデル保全、実画像/寸法、公開用ファイルへの個人パス/秘密値の非混入、文書参照、機器への非干渉を確認。課金API・MakerWorld公開・実印刷は行っていない。GUIでの再読込と現物検証は未実施。
