# 試験片30の実機ジョブ

更新: 2026-09-21 17:17 JST

## 依頼と対象

利用者の原文: 「作って」。直前に提案済みの最初の試し刷り（fit_30）を実行するものとして、冒頭で対象を伝えて進行。カービィ背面アダプターの最終版や3種類全部は印刷していない。

入力: `output/fit_30.stl`。出力: `output/kirby-fit30-X2D-04-PLA-PEI.3mf`。差し込み部36.4×12.4 mm、差し込み長20 mm、つば込み約45×21×23 mm。底面のつばを下に配置し、拡大縮小なし。

## 確認と実行

- Bambu Studio 02.08.02.61のGUIを使用。前の`molecular-first-trial-GUI-confirmed`は保存済み（保存メニュー無効）と確認し、上書きせず新規プロジェクトへ切替。
- プリンターは接続済みBambu Lab X2D。GUIの同期でメインノズル0.4 mm・標準流量、AMS接続を取得。AMS A1の黒PLA Basicを同期し、送信ダイアログでもA1対応を確認。
- プレートはTextured PEI Plateを設定。ライブカメラでテクスチャ付きプレートと、前の分子モデルが残っていない状態を確認。
- 0.16 mm Standard由来の設定で積層だけ0.20 mmに変更。初層0.20 mm、壁2周、サポートなし、自動ブリム、円弧フィッティング無効。115層、予測16分13秒、4.94 g。
- GUIが展開した設定の起動G-codeは14848文字、終了G-codeは2534文字。以前のCLI仮スライスのG-codeは使用していない。
- 送信設定: 自動ベッドレベリングOn、動的流量キャリブレーションAuto、ノズルオフセットOff、タイムラプスOff。
- 17:16頃、クラウド経由で送信。デバイス画面に今回のジョブ名が表示され、一時停止・中止が有効。0/115層、ベッド36/55℃、予定17:32を確認。これはジョブ受理・加熱開始であり、完成の証拠ではない。

## 検証と制限

PASS: GUIの読み込み・寸法表示、実機構成同期、スライス、送信先と材料割り当て、ジョブ受理・加熱。保存3MFのZIP CRC正常、機種・素材・積層設定を再読込して照合。

NOT_RUN: 印刷完了、完成品の寸法、クリーナーへの適合、保持力、漏気・吸引。カービィ背面の穴開け寸法はまだ確定しない。予測4.94 gを確定使用量として在庫減算していない。

保存3MFのSHA-256: `1cefa7bb9e91df6e31e2c67c7fa82153aff46a372bdbc6f828d4d1b5dfa18b54`。XML/JSON/config中に個人ホームパスなし。これは設定付きプロジェクトで、G-code入りの配布アーカイブではない。

変更は設定付きプロジェクトと記録のみ。CAD変更なしのため既存形状検査を再利用。TDD対象の実装変更なし。主担当が設定、対象、上書きなし、状態表現、秘密値・個人パス混入を自己レビュー。以前のClaude認証失敗は未解消のため独立レビュー済みとは扱わない。

## 操作上の失敗と復旧

新規ウィンドウのメニュー後に別ウィンドウが確認できず、保存済み状態を確認した上で新規プロジェクトへ切替。アプリbundle IDはインストール済みとマウントDMG内が重複したため、インストール済みアプリの絶対パスで選択。パスのpasteはタイムアウトし、ネイティブのsetValueで入力して復旧。プロファイル一覧が開かなかったため、表示された積層入力欄で0.20 mmを設定。送信ダイアログの最初の座標操作は送信に至らず、拡大されたダイアログを再観察して送信ボタンを操作し、送信進捗とプリンター側受理で実行を確認した。重複送信なし。

UIのツール結果はこの会話に存在するが、この記録はその内容の転記であり、全入出力・画面の自動全文保存ではない。秘密値と非公開の内部推論は含まない。
